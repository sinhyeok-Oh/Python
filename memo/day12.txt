변수
		저장공간과 값을 구분을 정확히 할 줄 알아야 한다

		data = 10 		// 저장공간
		data + 9		// 값
		data = data + 9	// 저장공간
		data - 1		// 값
		print(data)		// 값
		data = 20 + 9	// 저장공간

대입 연산자(복합 대입 연산자, 누적 연산자)
		+=, -=, *=, /=, %=, //=, ...

		money = 10000
		#money = money - 1000
		money -= 1000
		print(money)